{
  "scripts": {
    "dev": "python3 -m http.server 5173",
    "build": "rm -rf dist && mkdir -p dist/src && cp index.html dist/index.html && cp src/styles.css dist/src/styles.css"
  },
  "dependencies": {},
  "devDependencies": {}
}
